{% extends 'base.html.twig' %}
{% block content %}

    <!--banner-->
    <div class="banner-top">
        <div class="container">
            <h3 >Forum</h3>
            <!--<h4><a href="index.html">Home</a><label>/</label>Register</h4>-->
            <div class="clearfix"> </div>
        </div>
    </div>
    <br><br>
        <div class="login">
            <div class="main-agileits">
                <div class="form-w3agile form1">
                    <fieldset  style="margin-bottom: 10px">
                        <legend>{{ pub.idUser.nom  }} </legend>
                        <h3>{{ pub.titre }}</h3>
                        <p>{{ pub.datep|date }}</p>
                        <p>
                            <img src="{{ asset('images/')~pub.image }}" width="150">
                        </p>
                        <p>{{ pub.contenu }}</p>
                        {% if pub.idUser == u %}
                            <a href="{{ path('publication_delete', { 'id': pub.id }) }}"><i class="fa fa-plus-circle"></i> delete Publication</a>

                        {% endif %}

                        <br>
                        <br>

                        <form method="POST" name="formulaire">
                            <textarea name="contenu"></textarea>
                            <br>
                            <br>
                            <input type="text" value="{{ pub.id }}" name="id1" hidden>
                            <input type="submit" value="comment" class="btn btn-primary py-3 px-5">
                        </form>
                        <br>
                        <br>
                        {% for c in list1 %}
                            {% if c.idPublication == p.id %}
                                <fieldset style="margin-bottom: 0px">
                                    <legend>{{ c.iduser.nom }}</legend>
                                    <p>{{ c.datec|date }}</p>
                                    <p>{{ c.contenu }}</p>
                                    {% if c.iduser == u %}
                                        <a href="{{ path('commentaire_delete', { 'id': c.id }) }}"><i class="fa fa-plus-circle"></i> delete your comment</a>
                                    {% endif %}
                                </fieldset>

                            {% endif %}
                        {% endfor %}

                    </fieldset>
                </div>
            </div>
        </div>


    <br>
    <br>
    <br>
    <br>
{% endblock content %}