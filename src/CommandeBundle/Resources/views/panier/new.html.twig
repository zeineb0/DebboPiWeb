{% extends 'base.html.twig' %}

{% block body %}
    <div class="banner-top">
        <div class="container">
            <h3 >Création Panier</h3>
            <div class="clearfix"> </div>
        </div>
    </div>



    {% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}
    <div style="display: block; margin: 100px; ">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="add-1" type="submit" value="Create" />
    {{ form_end(form) }}

            <a style="width: 150px; margin-bottom: 10px;height: 30px; border:none" class=" add-1" href="{{ path('panier_index') }}">Back to the list</a>

</div>
{% endblock %}
