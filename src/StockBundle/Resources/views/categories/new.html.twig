{% extends 'base.html.twig' %}

{% block content %}
<div class="banner-top">
    <div class="container" >
        <h3 > <center>Categories </h3></center>
        <div class="clearfix"> </div>
    </div>
</div>
<div class="spec ">
    <h3>Ajouter une nouvelle categorie</h3>
    <div class="ser-t">
        <b></b>
        <span><i></i></span>
        <b class="line"></b>
    </div>

    <div class="login">
        <div class="main-agileits">
            < class="form-w3agile form1">


                {{ form_start(form) }}
                <label >Nom :</label>
                {{ form_widget(form.nom) }}
                {{ form_errors(form.nom) }}
                <br>
                <label >Choisissez un entrepot :</label>
                {{ form_widget(form.fkEntrepot) }}
                {{ form_errors(form.fkEntrepot) }}
                <br>
                <label >Ajouter l'image du produit :</label>
                {{ form_widget(form.imageFile) }}
                {{ form_errors(form.imageFile) }}
                <br>


        <input type="submit" value="Créer" /><br><br>

                <a href="{{ path('categories_index') }}">         <input type="submit" value="Retour à la liste" />
                </a>

                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}
